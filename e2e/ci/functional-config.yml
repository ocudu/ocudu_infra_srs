# SPDX-FileCopyrightText: Copyright (C) 2021-2026 Software Radio Systems Limited
# SPDX-License-Identifier: BSD-3-Clause-Open-MPI

inter_cu_ho:
  stage: mobility
  extends: .functional_e2e
  rules:
    - if: $CI_PIPELINE_SCHEDULE_DESCRIPTION =~ /functional/
    - when: manual
      allow_failure: true
  variables:
    KEYWORDS: "functional.mobility.inter_cu_ho."
    TESTBED: dynamic

inter_du_ho:
  stage: mobility
  extends: .functional_e2e
  rules:
    - if: $CI_PIPELINE_SCHEDULE_DESCRIPTION =~ /functional/
    - when: manual
      allow_failure: true
  variables:
    KEYWORDS: "functional.mobility.inter_du_ho."
    TESTBED: dynamic

inter_ru_ho:
  stage: mobility
  extends: .functional_e2e
  rules:
    - if: $CI_PIPELINE_SCHEDULE_DESCRIPTION =~ /functional/
    - when: manual
      allow_failure: true
  variables:
    KEYWORDS: "functional.mobility.inter_ru_ho."
    TESTBED: dynamic

intra_ru_ho:
  stage: mobility
  extends: .functional_e2e
  rules:
    - if: $CI_PIPELINE_SCHEDULE_DESCRIPTION =~ /functional/
    - when: manual
      allow_failure: true
  variables:
    KEYWORDS: "functional.mobility.intra_ru_ho."
    TESTBED: dynamic

paging:
  stage: mobility
  extends: .functional_e2e
  rules:
    - if: $CI_PIPELINE_SCHEDULE_DESCRIPTION =~ /functional/
    - when: manual
      allow_failure: true
  variables:
    KEYWORDS: "functional.mobility.paging."
    TESTBED: dynamic

reestablishment:
  stage: mobility
  extends: .functional_e2e
  rules:
    - if: $CI_PIPELINE_SCHEDULE_DESCRIPTION =~ /functional/
    - when: manual
      allow_failure: true
  variables:
    KEYWORDS: "functional.mobility.reestablishment."
    TESTBED: dynamic

cplane_traffic_load:
  stage: multiue
  extends: .functional_e2e
  rules:
    - if: $CI_PIPELINE_SCHEDULE_DESCRIPTION =~ /functional/
    - when: manual
      allow_failure: true
  variables:
    KEYWORDS: "functional.multiue.cplane_traffic_load."
    TESTBED: dynamic

mobility:
  stage: multiue
  extends: .functional_e2e
  rules:
    - if: $CI_PIPELINE_SCHEDULE_DESCRIPTION =~ /functional/
    - when: manual
      allow_failure: true
  variables:
    KEYWORDS: "functional.multiue.mobility."
    TESTBED: dynamic

qos:
  stage: multiue
  extends: .functional_e2e
  rules:
    - if: $CI_PIPELINE_SCHEDULE_DESCRIPTION =~ /functional/
    - when: manual
      allow_failure: true
  variables:
    KEYWORDS: "functional.multiue.qos."
    TESTBED: dynamic

simultaneous_udp:
  stage: multiue
  extends: .functional_e2e
  rules:
    - if: $CI_PIPELINE_SCHEDULE_DESCRIPTION =~ /functional/
    - when: manual
      allow_failure: true
  variables:
    KEYWORDS: "functional.multiue.simultaneous_udp."
    TESTBED: dynamic

slice:
  stage: multiue
  extends: .functional_e2e
  rules:
    - if: $CI_PIPELINE_SCHEDULE_DESCRIPTION =~ /functional/
    - when: manual
      allow_failure: true
  variables:
    KEYWORDS: "functional.multiue.slice."
    TESTBED: dynamic

traffic_model:
  stage: multiue
  extends: .functional_e2e
  rules:
    - if: $CI_PIPELINE_SCHEDULE_DESCRIPTION =~ /functional/
    - when: manual
      allow_failure: true
  variables:
    KEYWORDS: "functional.multiue.traffic_model."
    TESTBED: dynamic

ue_state_transition:
  stage: multiue
  extends: .functional_e2e
  rules:
    - if: $CI_PIPELINE_SCHEDULE_DESCRIPTION =~ /functional/
    - when: manual
      allow_failure: true
  variables:
    KEYWORDS: "functional.multiue.ue_state_transition."
    TESTBED: dynamic

bands:
  stage: singleue
  extends: .functional_e2e
  rules:
    - if: $CI_PIPELINE_SCHEDULE_DESCRIPTION =~ /functional/
    - when: manual
      allow_failure: true
  variables:
    KEYWORDS: "functional.singleue.bands."
    TESTBED: dynamic

bandwiths:
  stage: singleue
  extends: .functional_e2e
  rules:
    - if: $CI_PIPELINE_SCHEDULE_DESCRIPTION =~ /functional/
    - when: manual
      allow_failure: true
  variables:
    KEYWORDS: "functional.singleue.bandwiths."
    TESTBED: dynamic

drb_triggered_ul_grant:
  stage: singleue
  extends: .functional_e2e
  rules:
    - if: $CI_PIPELINE_SCHEDULE_DESCRIPTION =~ /functional/
    - when: manual
      allow_failure: true
  variables:
    KEYWORDS: "functional.singleue.drb_triggered_ul_grant."
    TESTBED: dynamic

emergency_call:
  stage: singleue
  extends: .functional_e2e
  rules:
    - if: $CI_PIPELINE_SCHEDULE_DESCRIPTION =~ /functional/
    - when: manual
      allow_failure: true
  variables:
    KEYWORDS: "functional.singleue.emergency_call."
    TESTBED: dynamic

fdd_siso:
  stage: singleue
  extends: .functional_e2e
  rules:
    - if: $CI_PIPELINE_SCHEDULE_DESCRIPTION =~ /functional/
    - when: manual
      allow_failure: true
  variables:
    KEYWORDS: "functional.singleue.fdd_siso."
    TESTBED: dynamic

gbr_vonr:
  stage: singleue
  extends: .functional_e2e
  rules:
    - if: $CI_PIPELINE_SCHEDULE_DESCRIPTION =~ /functional/
    - when: manual
      allow_failure: true
  variables:
    KEYWORDS: "functional.singleue.gbr_vonr."
    TESTBED: dynamic

mocn:
  stage: singleue
  extends: .functional_e2e
  rules:
    - if: $CI_PIPELINE_SCHEDULE_DESCRIPTION =~ /functional/
    - when: manual
      allow_failure: true
  variables:
    KEYWORDS: "functional.singleue.mocn."
    TESTBED: dynamic

nrppa:
  stage: singleue
  extends: .functional_e2e
  rules:
    - if: $CI_PIPELINE_SCHEDULE_DESCRIPTION =~ /functional/
    - when: manual
      allow_failure: true
  variables:
    KEYWORDS: "functional.singleue.nrppa."
    TESTBED: dynamic

ntn:
  stage: singleue
  extends: .functional_e2e
  rules:
    - if: $CI_PIPELINE_SCHEDULE_DESCRIPTION =~ /functional/
    - when: manual
      allow_failure: true
  variables:
    KEYWORDS: "functional.singleue.ntn."
    TESTBED: dynamic

phy_configs:
  stage: singleue
  extends: .functional_e2e
  rules:
    - if: $CI_PIPELINE_SCHEDULE_DESCRIPTION =~ /functional/
    - when: manual
      allow_failure: true
  variables:
    KEYWORDS: "functional.singleue.phy_configs."
    TESTBED: dynamic

power_control:
  stage: singleue
  extends: .functional_e2e
  rules:
    - if: $CI_PIPELINE_SCHEDULE_DESCRIPTION =~ /functional/
    - when: manual
      allow_failure: true
  variables:
    KEYWORDS: "functional.singleue.power_control."
    TESTBED: dynamic

pws:
  stage: singleue
  extends: .functional_e2e
  rules:
    - if: $CI_PIPELINE_SCHEDULE_DESCRIPTION =~ /functional/
    - when: manual
      allow_failure: true
  variables:
    KEYWORDS: "functional.singleue.pws."
    TESTBED: dynamic

security:
  stage: singleue
  extends: .functional_e2e
  rules:
    - if: $CI_PIPELINE_SCHEDULE_DESCRIPTION =~ /functional/
    - when: manual
      allow_failure: true
  variables:
    KEYWORDS: "functional.singleue.security."
    TESTBED: dynamic

slicing:
  stage: singleue
  extends: .functional_e2e
  rules:
    - if: $CI_PIPELINE_SCHEDULE_DESCRIPTION =~ /functional/
    - when: manual
      allow_failure: true
  variables:
    KEYWORDS: "functional.singleue.slicing."
    TESTBED: dynamic

tdd_mimo:
  stage: singleue
  extends: .functional_e2e
  rules:
    - if: $CI_PIPELINE_SCHEDULE_DESCRIPTION =~ /functional/
    - when: manual
      allow_failure: true
  variables:
    KEYWORDS: "functional.singleue.tdd_mimo."
    TESTBED: dynamic

tdd_siso:
  stage: singleue
  extends: .functional_e2e
  rules:
    - if: $CI_PIPELINE_SCHEDULE_DESCRIPTION =~ /functional/
    - when: manual
      allow_failure: true
  variables:
    KEYWORDS: "functional.singleue.tdd_siso."
    TESTBED: dynamic
