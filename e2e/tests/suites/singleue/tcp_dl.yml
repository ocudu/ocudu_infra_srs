# SPDX-FileCopyrightText: Copyright (C) 2021-2026 Software Radio Systems Limited
# SPDX-License-Identifier: BSD-3-Clause-Open-MPI

# Application level downlink throughput over TCP
# The purpose of this test is to measure the DL throughput a single UE reaches with TCP, where the
# uplink also carries the TCP acknowledgements. It is the TCP counterpart of singleue/udp_dl.

baseline:
  template: ue_simulator.test_gnb
  request: s72
  ue:
    config: [1cell.cfg, tcp_singleue.cfg]
    parameters:
      traffic_duration: 30
      nof_tcp_connections: 20 # Maximum, 10 were congestion window limited at ~32 kB each
      nof_antennas_dl: 4
      nof_antennas_ul: 1
      pdcch_decode_threshold: 0.0
      log_level: warning
  gnb:
    config: [cu_cp.yml, du.yml, tdd_default.yml, du_perf.yml, drb_triggered_ul_grant_qos.yml, schedtrace.yml]
    parameters:
      nof_antennas_dl: 4
      nof_antennas_ul: 1
      log_level: warning
  core:
    config: [baseline.cfg]
    parameters:
      log_level: warning
  feature_ids: [MVP-PERF-THR-1b]
  criteria:
    du.dl_bitrate_gt: 800.0e+6
    du.ul_bitrate_gt: 10.0e+6
    du.dl_avg_ri_ge: 3.5
    du.ul_avg_ri_ge: 1
    du.nof_ko_dl_le: 0
    du.nof_ko_ul_le: 0
    du.nof_reestablishments_request_eq: 0
    core.nof_pdu_session_establishment_accept_eq: 1
    du.nof_error_indications_le: 0
    all.errors_le: 0
    all.warnings_le: 0
