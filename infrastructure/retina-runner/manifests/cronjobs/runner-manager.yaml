#
# Copyright 2021-2026 Software Radio Systems Limited
#
# By using this file, you agree to the terms and conditions set
# forth in the LICENSE file which can be found at the top level of
# the distribution.
#

apiVersion: batch/v1
kind: CronJob
metadata:
  name: runner-manager-${organization_name}
  namespace: ${namespace}
spec:
  schedule: "*/1 7-19 * * 1-5"  # 7:00 to 19:59
  timeZone: "Europe/Madrid"
  concurrencyPolicy: Replace
  jobTemplate:
    spec:
      ttlSecondsAfterFinished: 50
      template:
        metadata:
          annotations:
            retinaRunnerJob: "true"
        spec:
          serviceAccountName: retina-runner-sa-${organization_name}
          containers:
            - name: runner-manager-script
              image: ${image_repository}:${image_tag}
              imagePullPolicy: IfNotPresent
              command: ["/bin/sh"]
              args:
                - "-c"
                - |
                  set -euo pipefail
                  python3 /usr/local/bin/retina_runner.py --incluster
              env:
                - name: RETINA_RUNNERS_CONFIGMAP
                  value: retina-runners-${organization_name}
                - name: GITLAB_TOKEN
                  valueFrom:
                    secretKeyRef:
                      name: gitlabtoken-${organization_name}
                      key: GITLAB_TOKEN
          imagePullSecrets:
            - name: registry-credentials
          tolerations:
            - key: ${taint_key}
              operator: "Equal"
              value: ${taint_value}
              effect: "NoSchedule"
          restartPolicy: Never
