# Viavi

This library provides a wrapper to call the VIAVI Restfull API from python. It also supports communication with a FPT server is the same machine to easily deploy campaigns/tests and recover test logs and reports.
